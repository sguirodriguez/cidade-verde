export default class City {
    name;
    balance;
    incomeRate;

    constructor({ name, balance, income }) {
        this.name = name;
        this.balance = balance;
        this.incomeRate = income;
    }

}

